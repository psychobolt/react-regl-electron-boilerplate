import { css } from 'styled-components';

export const header = css`
  flex: initial;
  align-items: center;

  .row {
    padding: 5px;
  }

  .label {
    margin-right: 5px;
    font-weight: bold;
  }

  x-select {
    min-width: 98px;
  }
`;
